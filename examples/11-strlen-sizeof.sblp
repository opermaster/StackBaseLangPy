
func print_array{
    /* *array */
    let int len
    let array nums
    let int i
        
    nums store
    nums deref sizeof len store
    len deref 1 - len store
    while {
        i deref len deref =<
        do
        nums deref i deref get deref 
        "%d " printf
        i deref 1 + i store
    }
    "\n" printf
}

func main{
     sizeof int "int = %d\n" printf
     sizeof float "float = %d\n" printf

     5 let [int] nums
     nums deref sizeof "sizeof(nums) = %d\n" printf
     nums deref print_array

     /*let string name
     "Kiril" name store
     name deref strlen "Name len is: %d\n" printf
     name deref 0 arr_get "%c" printf*/
}    
